import PropTypes from 'prop-types';

const CartInfo = () => {
    // const { title, price, credit } = cart;
    console.log()

    return (
        <div>




        </div>
    )
}

CartInfo.propTypes = {
    cart: PropTypes.object,
    courseTitle: PropTypes.array,
}
export default CartInfo;